# PWA setup

Install `wordpress/hausanovels-pwa-v0.1.1.zip`, save WordPress permalinks once, and verify these URLs:

- `/manifest.webmanifest`
- `/service-worker.js`
- `/offline/`
- `/.well-known/assetlinks.json`

The v0.1.1 package uses safer padded icons and a shorter TWA detection cookie.
